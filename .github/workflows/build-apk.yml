name: Build ZaibEx V3.4 APK

on:
  workflow_dispatch:
  push:
    paths:
      - 'ZaibEx_Android_V3_4_SEPARATE_APP.zip'
      - '.github/workflows/build-apk.yml'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout ZaibEx repository
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle 8.9
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Extract ZaibEx V3.4
        run: |
          rm -rf extracted
          mkdir -p extracted

          unzip -q ZaibEx_Android_V3_4_SEPARATE_APP.zip -d extracted

          echo "=== ZAIBEX PROJECT CONTENT ==="
          find extracted -maxdepth 3 -type f | sort | head -100

      - name: Verify ZaibEx project
        run: |
          test -f extracted/ZaibEx_Android_V3_4_SEPARATE_APP/settings.gradle.kts
          test -f extracted/ZaibEx_Android_V3_4_SEPARATE_APP/build.gradle.kts
          test -f extracted/ZaibEx_Android_V3_4_SEPARATE_APP/app/build.gradle.kts

          echo "ZaibEx V3.4 project verified"

      - name: Verify separate ZaibEx package
        run: |
          grep -q 'applicationId = "ae.zaibex.app"' \
          extracted/ZaibEx_Android_V3_4_SEPARATE_APP/app/build.gradle.kts

          echo "Confirmed: ZaibEx uses ae.zaibex.app"
          echo "It will not replace NadEx."

      - name: Build ZaibEx V3.4 APK
        working-directory: extracted/ZaibEx_Android_V3_4_SEPARATE_APP
        run: |
          gradle clean assembleDebug --stacktrace

      - name: Verify APK generated
        run: |
          APK="extracted/ZaibEx_Android_V3_4_SEPARATE_APP/app/build/outputs/apk/debug/app-debug.apk"

          if [ ! -f "$APK" ]; then
            echo "ERROR: ZaibEx APK was not generated"
            exit 1
          fi

          echo "ZaibEx V3.4 APK successfully generated"
          ls -lh "$APK"

      - name: Upload ZaibEx V3.4 APK
        uses: actions/upload-artifact@v4
        with:
          name: ZaibEx-V3-4-Separate-App-APK
          path: extracted/ZaibEx_Android_V3_4_SEPARATE_APP/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
          retention-days: 30
